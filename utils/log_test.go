package utils

import (
	"testing"
)

func TestLog(t *testing.T) {
	/*
		Rewrite better test cases for this later
		logFile := InitLogging()
		defer logFile.Close()

		log.Print("[+] Uncolored log message")
		log.Print(text.FgGreen.Sprint("[+] Green log message"))
		log.Print(text.FgYellow.Sprint("[+] Yellow log message"))
		log.Print(text.FgRed.Sprint("[+] Red log message"))
	*/
}
